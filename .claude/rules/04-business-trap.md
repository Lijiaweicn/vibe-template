# 04 - 项目特定业务与历史遗留坑点防护

## 一、 遗留代码演进规则

1. **彻底禁用 Mixins**：在维护、重构老旧的 Vue 2 相关混合页面时，**绝对禁止**引入新的 Mixins 或者是修改既有 Mixins 来共享状态。所有的共享判定逻辑，必须提取为纯 JS/TS 文件导出，并在组件中引入。
2. **大客户定制功能长期共存原则**：项目代码库中包含大量针对特定大客户（如某些特定燃料采购方、特定招标方）的高度客制化业务逻辑（Feature Toggles）。在进行全局重构时，**绝对禁止**删除或顺手重构掉这些看似“冗余”的代码块，它们需要长期共存。
